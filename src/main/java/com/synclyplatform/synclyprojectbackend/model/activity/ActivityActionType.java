package com.synclyplatform.synclyprojectbackend.model.activity;

public enum ActivityActionType {
    CREATED, PROMOTED, RESOLVED,
    REJECTED, DELETED, BLOCKED,
    UPDATED, CHANGED_CATEGORY, ENABLED,
    DISABLED
}

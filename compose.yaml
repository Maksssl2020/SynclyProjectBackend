services:
  postgres:
    image: postgres
    environment:
      POSTGRES_DB: syncly-db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Leszczu2039#
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: